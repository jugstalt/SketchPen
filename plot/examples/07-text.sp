// text
// -----
// Draws a text string at a reference point (x,y), at the given font size
// (logical units, same coordinate space as everything else), using the
// current pen/brush color or an optional inline override.

text.draw("Aa", 36, 0, 12);
text.draw("42", 20, 0, -18, "#e0592a");
