// Comments are free-form text: it's fine to use apostrophes, "quotes" and ; semicolons in them.
circle.fill(40); // a trailing comment: don't panic
// A comment in the middle of the file, e.g. one that won't break lexing.
circle.draw(40); // "quoted" text and an unbalanced ' quote
