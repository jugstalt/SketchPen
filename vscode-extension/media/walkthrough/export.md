Export the active icon as a single SVG/PNG, or as a full package — batch PNG ZIP, web sprite sheet, or a themeable CSS-variable HTML page.

[Export Package…](command:sketchpen.exportPackage)

Also available via `Export as SVG` / `Export as PNG`, the editor title bar, or the Explorer right-click menu.
