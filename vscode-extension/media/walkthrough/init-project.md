Scaffold a new icon set: a working `styles/default.globals`, a `templates/` folder with an example `.spt`, a starter `icon.sp` that already renders something, and an `AGENTS.md` for AI coding agents.

[Run SketchPen: Init](command:sketchpen.init)

Safe to re-run — it never overwrites a file that already exists.
