SketchPen shells out to the **`sketchpen` CLI** for rendering, completion data, and diagnostics — it does no compiling itself.

```bash
dotnet tool install -g SketchPen.Cli
```

[Install the CLI](command:sketchpen.installCli)

Already installed somewhere not on `PATH`? Set `sketchpen.cliPath` in your settings instead.
