Open a `.sp` file and save it — a preview panel opens automatically and re-renders on every save. Errors show up as red squiggles on the offending line, including inside `#include`d `.spt` files.

[Preview the active icon](command:sketchpen.preview)

You can also trigger this from the editor title bar icon or the Explorer right-click menu.
